@using Pronia.Extencions.Enums;
@model IEnumerable<Tag>
@{
    int count = 0;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Tag Table</h4>
            <a class="btn btn-success " asp-action="create" asp-controller="tag">Create</a>

            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                           
                            <th>
                                Tag Name
                            </th>
                            
                           
                            <th>
                                Settings
                            </th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Tag tag in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                              
                                <td>
                                    @tag.Name
                                </td>
                               

                                <td>
                                    <a class="btn btn-warning" asp-controller="tag" asp-action="update" asp-route-id="@tag.Id">
                                        <i class="mdi mdi-lead-pencil"></i>
                                    </a>
                                    <a class="btn btn-danger" asp-controller="tag" asp-action="delete" asp-route-id="@tag.Id">
                                        <i class="mdi mdi-delete"></i>
                                    </a>
                                    <a class="btn btn-primary" asp-controller="tag" asp-action="detail" asp-route-id="@tag.Id">
                                        <i class="mdi mdi-logout-variant"></i>
                                    </a>
                                </td>

                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
